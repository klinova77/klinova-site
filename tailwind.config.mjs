export default {
  theme: {
    extend: {
      fontFamily: {
        // Texte par défaut : pile système (aucun chargement réseau)
        sans: ['ui-sans-serif','system-ui','-apple-system','Segoe UI','Roboto','Helvetica','Arial'],
        // Header uniquement : Caviar Dreams + fallback
        caviar: ['"Caviar Dreams"', 'ui-sans-serif','system-ui'],
      },
    },
  },
};
